n=1
speed=2
force=5

size_x = 125
size_y = 100

move_joint(demo_safe)
jump(cylinder)

#Flat
flat:
move_linear(demo_flat)
set_part_geometry(FLAT,0)

# Lift rects 
repeat1f:
grind_rect({size_x},{size_y},{n},{speed},{force},0)
size_x -= 40
size_y -= 20
jump_gt_zero(size_x,repeat1f)

size_x = 125
size_y = 100

# No Lift Rects
repeat2f:
grind_rect({size_x},{size_y},{n},{speed},{force},1)
size_x -= 40
size_y -= 20
jump_gt_zero(size_x,repeat2f)

move_linear(demo_safe)

cylinder:
move_linear(demo_cyl)
set_part_geometry(CYLINDER,1200)

size_x = 85
size_y = 100

# Lift rects 
repeat1c:
grind_rect({size_x},{size_y},{n},{speed},{force},0)
size_x -= 25
size_y -= 20
jump_gt_zero(size_x,repeat1c)

size_x = 85
size_y = 100

# No Lift Rects
repeat2c:
grind_rect({size_x},{size_y},{n},{speed},{force},1)
size_x -= 25
size_y -= 20
jump_gt_zero(size_x,repeat2c)

sphere:
move_linear(demo_sphere)
set_part_geometry(SPHERE,1200)

size_x = 85
size_y = 100

# Lift rects 
repeat1s:
grind_rect({size_x},{size_y},{n},{speed},{force},0)
size_x -= 25
size_y -= 20
jump_gt_zero(size_x,repeat1s)

size_x = 85
size_y = 100

# No Lift Rects
repeat2s:
grind_rect({size_x},{size_y},{n},{speed},{force},1)
size_x -= 25
size_y -= 20
jump_gt_zero(size_x,repeat2s)
