set_speed(0.5)
set_accel(1.0)
set_blend(0.003)

retry:
select_tool(2F85)

count=2
loop1:

movepose(spheretop10)
set_part_geometry(SPHERE,82)
grind_rect(20,20,2)

movepose(cylcenter10)
set_part_geometry(CYLINDER,77)
grind_rect(40,40,2)

movepose(clamp10)
set_part_geometry(FLAT,0)
grind_rect(10,10,2)

count--
jump_gt_zero(count,loop1)

select_tool(angletest)

count=2
loop2:
movepose(spheretop10)
set_part_geometry(SPHERE,82)
grind_rect(20,20,2)

movepose(cylcenter10)
set_part_geometry(CYLINDER,77)
grind_rect(40,40,2)

movepose(clamp10)
set_part_geometry(FLAT,0)
grind_rect(10,10,2)

count--
jump_gt_zero(count,loop2)

jump(retry)
