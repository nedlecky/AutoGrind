clear()

count=1
speed=10
force=1

grind_trial_speed({speed})
grind_touch_retract(10)
grind_touch_speed(10)

cmd=grind_rect(60,40,{count},{speed},{force},0)

set_part_geometry(FLAT,0)
move_linear(oper1)
{cmd}
